[project]
name = "rag-mcp"
version = "0.1.0"
description = "MCP RAG Agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "faiss-cpu>=1.11.0",
    "markitdown>=0.1.1",
    "tqdm>=4.67.1",
]
